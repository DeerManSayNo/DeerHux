{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-header",
  "description": "Synchronize native header controls from the main desktop UI",
  "windows": ["main"],
  "remote": { "urls": ["http://localhost:30141", "http://127.0.0.1:*"] },
  "permissions": ["allow-sync-header-controls"]
}
