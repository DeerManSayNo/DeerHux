# 滑动条

本系统预留给数值 Slider，不包含滚动条。

## 当前规则

- 当前产品没有已确认的共享 Slider 需求，也没有可复用组件。
- 出现需求后先明确单值或区间、min/max/step、格式化、标签、方向和受控 API。
- 实现必须同时覆盖键盘、指针和触摸输入，提供可访问名称、当前值与禁用状态。

## 引用

- 交互公共规则：`docs/design-system/systems/interaction.md`。

## 例外或待确认

状态为待补齐。不得把现有滚动条样式当作 Slider 规范，也不为推测需求预建组件。
